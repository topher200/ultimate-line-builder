import { ScreenStub } from '../components/ScreenStub.tsx';

export function PredictorScreen() {
  return (
    <ScreenStub
      title="Predictor"
      blurb="Simulate a game assuming O and D points trade; filter O-only / D-only. Later iteration."
    />
  );
}
