/** Points a game is expected to last, used until a game sets its own value. */
export const DEFAULT_EXPECTED_POINTS = 20;

/** Default team names for a new game, editable per game. */
export const DEFAULT_OUR_TEAM = 'Rampage';
export const DEFAULT_THEIR_TEAM = 'Opponent';
